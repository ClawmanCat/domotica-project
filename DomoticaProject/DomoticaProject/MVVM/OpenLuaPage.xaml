<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DomoticaProject.MVVM.OpenLuaPage">
    <ContentPage.Content>
          <ListView HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" x:Name="FileList" ItemSelected="FileSelected">
               <ListView.ItemTemplate>
                    <DataTemplate>
                         <ViewCell>
                              <StackLayout>
                                   <Label Text="{Binding Title}" FontSize="Large" FontAttributes="Bold"></Label>
                                   <Label Text="{Binding Desc}"></Label>
                              </StackLayout>
                         </ViewCell>
                    </DataTemplate>
               </ListView.ItemTemplate>
          </ListView>
    </ContentPage.Content>
</ContentPage>