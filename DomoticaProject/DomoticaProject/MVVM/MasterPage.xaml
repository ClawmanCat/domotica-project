<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DomoticaProject.MVVM.MasterPage">

     <MasterDetailPage.Master>
          <ContentPage Title="Master" BackgroundColor="LightGray">
               <ListView x:Name="MasterMenuItems" ItemTapped="MenuItemTapped">
                    <ListView.ItemTemplate>
                         <DataTemplate>
                              <ViewCell>
                                   <StackLayout Orientation="Horizontal" Padding="5">
                                        <Label Text="{Binding Icon}" FontFamily="{StaticResource IconFont}" FontSize="20" VerticalOptions="Center"></Label>
                                        <Label Text="{Binding Text}" FontSize="20" VerticalOptions="Center"></Label>
                                   </StackLayout>
                              </ViewCell>
                         </DataTemplate>
                    </ListView.ItemTemplate>
               </ListView>
          </ContentPage>
     </MasterDetailPage.Master>

     <MasterDetailPage.Detail>
          <NavigationPage>
               <x:Arguments>
                    <ContentPage Title="DetailPage" />
               </x:Arguments>
          </NavigationPage>
     </MasterDetailPage.Detail>
</MasterDetailPage>