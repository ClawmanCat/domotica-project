<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:DomoticaProject"
             x:Class="DomoticaProject.MainPage">

    <StackLayout>
          <ListView ItemTapped="CommandItemTapped" x:Name="CommandList">
               <ListView.ItemTemplate>
                    <DataTemplate>
                         <ViewCell>
                              <StackLayout>
                                   <Label Text="{Binding name}" FontSize="32" FontAttributes="Bold" HorizontalOptions="Center"></Label>

                                   <StackLayout Orientation="Horizontal" HorizontalOptions="Center">
                                        <Label Text="ID = "></Label>
                                        <Label Text="{Binding id}"></Label>
                                   </StackLayout>
                              </StackLayout>
                         </ViewCell>
                    </DataTemplate>
               </ListView.ItemTemplate>
          </ListView>

          <ListView x:Name="ResponseList"></ListView>
    </StackLayout>

</ContentPage>
